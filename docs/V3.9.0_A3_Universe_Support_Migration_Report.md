# V3.9.0 A3 Universe Support Migration Report

## 1. Candidate Audit

| Module | Role | Writes artifact | CLI | Authority | Import fan-out | Risk | A3 decision |
|---|---|---:|---:|---|---:|---|---|
| `universe_loader.py` | canonical membership loading and validation | NO | YES | Membership | 10 | HIGH | DEFER |
| `universe_manager.py` | legacy/single-file membership validation and loading | NO | YES | Membership | 4 | HIGH | DEFER |
| `universe_groups.py` | group configuration and combined membership | NO | YES | Membership | 1 | HIGH | DEFER |
| `universe_metadata.py` | artifact UniverseVersion tagging and compatibility classification | NO | NO | Support only | 4 | LOW-MEDIUM | MIGRATE |
| `universe_scale_test.py` | research scale validation/download orchestration | NO critical artifact | YES | Research CLI | 0 | MEDIUM | DEFER |
| `watchlist.py` | compatibility access to primary membership | NO | NO | Membership-facing wrapper | 4 | HIGH | DEFER |
| `data_readiness.py` | readiness classification and quality artifact publication | YES | YES | Readiness | 5 | HIGH | DEFER |
| `universe_source.py` | active universe mode selection and validation | NO | YES | Membership source | 4 | HIGH | DEFER |

Fan-out counts direct imports from root Python modules. `universe_metadata.py` is
the only candidate that does not select membership, construct the research
population, classify readiness, publish a critical runtime artifact, or own a
CLI. Its config import reads the single canonical UniverseVersion authority; it
does not modify config or create another version authority.

## 2. Selected Module

`universe_metadata.py` only.

Public surface preserved:

- Functions: `universe_compatibility`, `tag_current_universe`,
  `dataframe_universe_compatibility`
- Constants: `PRIMARY_UNIVERSE_VERSION`, `UNIVERSE_VERSION_FIELD`, `MATCH`,
  `MISMATCH`, `MISSING`
- Classes/exceptions: none declared; existing `TypeError` behavior is preserved

## 3. Deferred High-Risk Modules

- `universe_loader.py`: canonical path, membership, normalization, validation,
  ordering, duplicate/empty rejection, and CLI authority.
- `universe_manager.py`, `universe_groups.py`, `universe_source.py`, and
  `watchlist.py`: select or expose membership and active-source semantics.
- `data_readiness.py`: owns READY and exclusion classification plus the quality
  artifact.
- `universe_scale_test.py`: user-facing research CLI and download orchestration.

## 4. Before / After Structure

Before:

```text
universe_metadata.py                 # implementation
```

After:

```text
src/universe/__init__.py
src/universe/universe_metadata.py    # sole implementation
universe_metadata.py                 # compatibility re-exports only
```

## 5. Universe Authority Unchanged

The formal membership authority remains
`data/AI_investing_universe_150_V2.csv` through the existing config and
canonical loader contract. `PRIMARY_UNIVERSE_VERSION` remains
`AI_investing_universe_150_V2`. A3 does not move or edit the membership file,
loader, config, or normalization/validation logic.

The regression result is configured 150, unique 150, duplicate 0, empty 0,
with the `order` column still exactly `1..150`.

## 6. Public API Compatibility

Runtime tests prove root and canonical functions are identical objects, their
signatures match, constants are identical objects, successful results match,
ordering is preserved, missing-version behavior is unchanged, and invalid
non-DataFrame inputs raise the same `TypeError`.

## 7. Root Wrapper

The root module imports and re-exports the canonical public surface and defines
only `__all__`. An AST test rejects root function, async-function, and class
definitions and rejects assignments other than `__all__`.

## 8. No Parallel Authority

There is one UniverseVersion metadata implementation in
`src/universe/universe_metadata.py`. The root module is a compatibility wrapper,
not an implementation. There remains one membership authority: the existing
CSV plus canonical loader/config contract.

## 9. Membership Contract Regression

- Configured: 150
- Unique: 150
- Duplicate: 0
- Empty: 0
- Ordering: preserved (`order == 1..150`)
- UniverseVersion: `AI_investing_universe_150_V2`
- First five: `SKHY`, `MAZE`, `GPCR`, `TSHA`, `BTU`
- Last five: `OKLO`, `CRWD`, `ISRG`, `IONQ`, `IBM`

## 10. Data Readiness Boundary

`data_readiness.py` was not modified. READY, INSUFFICIENT_HISTORY,
PROVIDER_REJECTED, STALE_MARKET_DATA, INVALID_CANONICAL_DATA, partial-readiness,
and READY filtering semantics remain owned by the existing readiness authority.
Research population remains Universe150 intersected with READY.

## 11. Artifact Stability

No artifact path or filename changed. The primary universe CSV,
`results/universe150_data_quality.csv`, score, candidate, risk, portfolio,
sizing, order, and report paths are unchanged. Runtime outputs generated by the
required pipeline remain governed/ignored runtime artifacts and are not part of
the A3 source diff.

## 12. Caller Impact

No caller import was edited. `combined_scoring.py`, `rank_stocks_v2.py`,
`production_candidate_builder.py`, and `report_artifact_consistency.py` continue
to import the root module surface. Compatibility tests prove their imported
function references resolve to the canonical implementation.

## 13. Tests

- Focused plus Universe/readiness contract tests: 59 passed, 0 failed
- Full suite: 1131 passed, 0 failed (1126 baseline plus 5 A3 tests)
- Compilation: passed for wrapper, canonical module, package initializer, and
  new test

## 14. Pipeline

`python run_all.py` passed 20 of 20 stages.

- RunId: `candidate-20260814-e0f2a993f765`
- AsOfDate: `2026-08-14`
- Configured: 150
- Ready: 148
- Excluded: 2 (`SKHY`, `SPCX`; insufficient history)
- BUY/WATCH/IGNORE: 0/4/144
- Status: `NO_ACTION`

## 15. BUY-Day Replay Decision

Not required. The migrated helper attaches and checks metadata only; it does not
normalize membership, filter candidates, construct the research population, or
decide readiness. The production membership and score/signal paths were not
moved or edited.

## 16. READY Population Note

Minimum READY count or coverage-threshold maintenance remains deferred. A3 does
not implement it and does not change the current 148-ready/2-excluded semantics.

## 17. Scope Verification

- Production logic changed: NO
- Universe membership changed: NO
- Universe ordering changed: NO
- UniverseVersion changed: NO
- Data readiness logic changed: NO
- READY filtering changed: NO
- Score formula changed: NO
- Threshold changed: NO
- Candidate eligibility changed: NO
- Risk formula changed: NO
- Portfolio policy changed: NO
- Order policy changed: NO
- Artifact path changed: NO
- Config changed: NO
- PROJECT_VERSION changed: NO
- Root CLI changed: NO
- Parallel Universe authority created: NO

## Recommendation

`A3 COMPLETE — READY FOR NEXT ARCHITECTURE MIGRATION`
